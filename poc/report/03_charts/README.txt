Charts are generated here by analyse_results.py.

Expected files after a full benchmark run:
  latency_local.png      — Bar chart: warm median latency per query, local mode (log scale)
  latency_gcs.png        — Bar chart: warm median latency per query, GCS mode (log scale)
  write_throughput.png   — Bar chart: rows/s for W1–W4 per engine
