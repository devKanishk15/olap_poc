# GCS read benchmark runner — minimal dependencies
# Install: pip install -r requirements.txt
# (If the parent harness venv is already active, these are already installed.)

# DuckDB in-process engine + Python API
duckdb>=1.2.0,<2.0

# Doris / MySQL protocol connector
mysql-connector-python>=8.3,<9.0

# ClickHouse HTTP client
requests>=2.31,<3.0

# .env file loading
python-dotenv>=1.0,<2.0

# Summary table formatting
tabulate>=0.9,<1.0

# Rich console output (progress indicators, colour)
rich>=13.0,<14.0
